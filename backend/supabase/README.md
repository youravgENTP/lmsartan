# Supabase Backend

Planned components:

- Postgres tables for seen Canvas items and watcher runs
- check-canvas Edge Function
- Supabase Secrets for Canvas and ntfy credentials
- Cron job to invoke the watcher periodically
